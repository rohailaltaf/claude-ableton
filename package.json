{
  "name": "claude-ableton",
  "version": "0.1.14",
  "description": "MCP server letting Claude drive Ableton Live 12 via AbletonOSC",
  "license": "MIT",
  "author": "Rohail Altaf",
  "type": "module",
  "bin": {
    "claude-ableton": "dist/index.js"
  },
  "files": [
    "dist",
    "vendor"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "rm -rf dist && node scripts/build.mjs",
    "dev": "tsx src/index.ts",
    "vendor": "node scripts/vendor-remote-script.mjs",
    "package": "npm run vendor && npm run typecheck && npm run build"
  },
  "comment": "No runtime dependencies: esbuild inlines everything into dist/index.js, so the bundle runs with zero installs (fast `npx github:...`).",
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@types/node": "^22.0.0",
    "esbuild": "^0.28.0",
    "tonal": "^6.4.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "zod": "^3.25.0"
  }
}
