# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
*.egg-info/
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store

# Node
node_modules/
*.tsbuildinfo

# Note: dist/ (the bundled server) and vendor/AbletonOSC (the pinned Remote
# Script) are COMMITTED — the Claude plugin ships them and runs them directly
# (no npm install / build at plugin-install time). Regenerate with:
#   npm run package   (vendor + typecheck + build)

# Project
.env
.env.local
